<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if cart.size > 0 %>
    <%= render 'cart' %>
  <% else %>
    <%= 'Cart is empty go to' %>
    <%= link_to 'home page', '/' %>
  <% end %>

</section>
